/** The hosted pyRPC documentation MCP endpoint (Streamable HTTP). */
export const REMOTE_MCP_URL = "https://mcp.pyrpc.com/mcp";

/** Server key written into client configurations for the remote docs MCP. */
export const SERVER_NAME = "pyrpc-docs";
