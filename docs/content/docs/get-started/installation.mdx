---
title: Installation
description: Install pyRPC and its modular adapters.

---

pyRPC follows a modular packaging strategy. You only pay for what you use.

## Core Package

The tiny core protocol and runtime. This is always required.

```bash
# Using uv
uv add pyrpc-core

# Using pip
pip install pyrpc-core
```

## Adapters

Install the adapter for your favorite web framework.

### FastAPI

```bash
uv add pyrpc-fastapi
```

### Flask

```bash
uv add pyrpc-flask
```

## CLI & Code Generation

The `pyrpc` CLI comes built-in with `pyrpc-core` - no separate install needed.

### Dev Server (Recommended)

The fastest way to get started:

```bash
pyrpc dev
```

On first run, `pyrpc dev` walks you through setup interactively - framework, Python module, and TypeScript client path. It creates a `pyrpc.json` config file, generates types, and starts the development server with auto-regeneration on file changes.

You can also pass flags to skip the wizard:

```bash
pyrpc dev --framework fastapi --entry server --client-root ../frontend
```

### Configuration (`pyrpc.json`)

pyRPC stores its project configuration in a dedicated `pyrpc.json` file (not in `pyproject.toml`):

```json
{
  "version": 1,
  "framework": "fastapi",
  "entrypoint": "server",
  "client_root": "../frontend"
}
```

The file is created automatically by `pyrpc dev`. Paths in `pyrpc.json` are resolved relative to the config file's directory.

### Manual Codegen

For CI/CD or one-off generation:

```bash
pyrpc codegen http://localhost:8000
```

## Quick Start Example (FastAPI)

```python
from fastapi import FastAPI
from pyrpc_core import rpc
from pyrpc_fastapi import mount_fastapi

app = FastAPI()

@rpc
def add(a: int, b: int) -> int:
    return a + b

mount_fastapi(app)
```

## Next

- [Quickstart](/docs/get-started/quickstart) - Full server + client example
- [Concepts](/docs/concepts) - Mental model and protocol design
