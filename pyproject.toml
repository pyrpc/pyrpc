[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyrpc-workspace"
version = "0.1.0"
description = "pyRPC Monorepo Workspace"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "fastapi>=0.100.0",
    "flask>=3.0.0",
    "httpx>=0.24.0",
    "typer[all]>=0.9.0",
    "rich>=13.0.0",
    "jinja2>=3.1.0",
    "uvicorn>=0.23.0",
    "anyio>=4.0",
    "pydantic>=2.0",
]



[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
pyrpc = { workspace = true }
pyrpc-fastapi = { workspace = true }
pyrpc-flask = { workspace = true }
pyrpc-codegen = { workspace = true }


[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.ruff]
select = ["E", "F", "B", "I"]
line-length = 88
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
    "packages/pyrpc/src",
    "packages/pyrpc-fastapi/src",
    "packages/pyrpc-flask/src",
    "packages/pyrpc-codegen/src"
]
asyncio_mode = "auto"
