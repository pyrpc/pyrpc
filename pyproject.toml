[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyrpc-workspace"
version = "0.14.0"
description = "pyRPC Monorepo Workspace"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.25.0",
    "fastapi>=0.100.0",
    "flask>=3.0.0",
    "httpx>=0.24.0",
    "typer>=0.9.0",
    "rich>=13.0.0",
    "jinja2>=3.1.0",
    "uvicorn>=0.23.0",
    "anyio>=4.0",
    "pydantic>=2.0",
]



[tool.uv.workspace]
members = [
    "packages/pyrpc-core",
    "packages/pyrpc-codegen",
    "packages/pyrpc-django-adapter",
    "packages/pyrpc-fastapi",
    "packages/pyrpc-flask",
]

[tool.uv.sources]
pyrpc-core = { workspace = true }
pyrpc-django-adapter = { workspace = true }
pyrpc-fastapi = { workspace = true }
pyrpc-codegen = { workspace = true }
pyrpc-flask = { workspace = true }


[tool.hatch.build.targets.wheel]
bypass-selection = true

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "B", "I", "TCH", "SIM", "UP", "YTT"]
# E501: line length is not currently enforced; adopt alongside a formatter.
# E701/E702: single-line compound statements are an accepted CLI style here.
ignore = ["E501", "E701", "E702", "SIM108", "SIM117"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["SIM"]

[tool.mypy]
python_version = "3.11"
files = [
    "packages/pyrpc-core/src",
    "packages/pyrpc-codegen/src",
    "packages/pyrpc-fastapi/src",
    "packages/pyrpc-flask/src",
    "packages/pyrpc-django-adapter/src",
]
# Tests are a follow-up; src is the shipped surface.
exclude = ["tests"]

[[tool.mypy.overrides]]
module = ["jsonc_edit.*", "jsonschema_ts.*", "django.*", "pyrpc_codegen.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = [
    "packages/pyrpc-core/tests",
    "packages/pyrpc-django-adapter/tests",
    "packages/pyrpc-fastapi/tests",
    "packages/pyrpc-flask/tests",
    "packages/pyrpc-codegen/tests",
]
pythonpath = [
    "packages/pyrpc-core/src",
    "packages/pyrpc-django-adapter/src",
    "packages/pyrpc-fastapi/src",
    "packages/pyrpc-flask/src",
    "packages/pyrpc-codegen/src"
]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "django>=4.2.0",
    "fastapi>=0.128.7",
    "flask>=3.1.2",
    "jinja2>=3.1.6",
    "mcp>=2.1,<3",
    "mypy>=2.3.1",
    "pydantic-core==2.48.0",
    "pyrpc-codegen",
    "pyrpc-django-adapter",
    "pyrpc-fastapi",
    "pyrpc-flask",
    "pytest>=9.0.2",
    "pytest-asyncio>=0.25.0",
    "pytest-cov>=7.1.0",
    "rich>=14.3.2",
    "ruff>=0.16.4",
    "typer>=0.21.1",
    "uvicorn>=0.40.0",
]
